# Build the Docker image
docker build -t fullstack .

# Run the container in attached mode (Helpful for viewing logs)
docker run --name fullstackContainer -p 3000:3000 fullstack

## TO exit use another terminal window and pass the stop command 